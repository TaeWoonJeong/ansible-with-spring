## 스프링코드

```
https://github.com/TaeWoonJeong/ansible-with-spring-code
```

## 사용법

```
1. git clone을 한다.
2. Vagrantfile의 IP 와 bridge를 바꾼다.
3. ./init/ansible-server.yml 의 IP를 바꾼다.
4. ./spring/application.yml 에서 IP를 바꾼다.
5. vagrant up 을 한다.
```
